//Kunal Mukherjee
//CS 210
//27th Sept , 2016
//Assn 05 - Simple Calculator

#include<stdio.h>
#include<math.h>
#include<stdlib.h>

void Instructions();
void GetInput(double *number, char *operator_c);
void DoOperation(double number, char operator_c, double *accumulator);

int main()
{
    double number, accumulator;
    char operator_c;
    accumulator = 0;

    Instructions();    
    GetInput( &operator_c, &number);

   do
    {
        DoOperation(number, operator_c, &accumulator);
        GetInput( &operator_c, &number);
    }
    while (operator_c != 'q');
        
         printf("\nThe program has ended and the last number calculated was %f\n", accumulator);
     return 0; 

}

void Instructions()
{
     printf("Enter a operator along with a number on a single line.  \nThe program will perform the operation indicated, show the result, \nand continue until the you enter(q) to exit."); 
}

void GetInput(char *operator_c, double *number)
{
    double number_input;
    char operator_input;

    printf("\n Enter a Arithmetic Operator and Number--> \t");  
    scanf_s("%c ", &*operator_c);
    scanf_s("%lf", &*number);
    return 0;
}

void DoOperation(double number, char operator_c, double *accumulator)
{
    
      switch (operator_c) {
        case '+':
            *accumulator += number; 
            printf(" %f \n",*accumulator );
            return 0;
            break;

        case '-':
            *accumulator -= number; 
            printf(" %f \n",*accumulator );
            return 0;
            break;

        case '*':
            *accumulator *= number; 
            printf(" %f \n",*accumulator );
            return 0;
            break;

        case '/':
            *accumulator /= number; 
            printf(" %f \n",*accumulator );
            return 0;
            break;             
    }  
  }

operator_c != 'q'

    

